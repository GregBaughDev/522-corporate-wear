<?php
    // var_dump($_SESSION['user'][0]['id']);
    // SQL IN PROGRESS
//     SELECT o.customerId, o.departmentId, item.catalogueId, item.orderId, dept.name
// FROM orders as o 
// INNER JOIN order_items as item
// USING (customerId)
// INNER JOIN department as dept
// USING (customerId)
// WHERE customerId = 1

?>